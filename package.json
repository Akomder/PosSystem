{
  "name": "pos-demo",
  "version": "1.0.0",
  "description": "POS System — Railway all-in-one deployment",
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client",
    "build":   "npm run build --prefix client",
    "start":   "npm start --prefix server"
  },
  "engines": {
    "node": ">=20"
  }
}
